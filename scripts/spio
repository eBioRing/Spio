#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
BUILD_DIR="${SPIO_BUILD_DIR:-$ROOT/build-codex}"
BIN="$BUILD_DIR/bin/spio"

if [[ ! -x "$BIN" ]]; then
  cmake -S "$ROOT" -B "$BUILD_DIR"
  cmake --build "$BUILD_DIR" --target spio
fi

exec "$BIN" "$@"
